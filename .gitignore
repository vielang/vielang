node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase service account key (NEVER commit)
*-firebase-admin*.json

# IDE & local data
.vscode/
/data/

# Next.js
.next/
next-env.d.ts
*.tsbuildinfo

# Claude Code local agent state
.claude/

# Test artifacts (Playwright e2e runs)
test-results/
playwright-report/
.playwright/
e2e/artifacts/

# Local credentials notes — NEVER commit (contains seeded admin/owner passwords)
docs/acc.txt
dev.txt

# LiveKit prod config — rendered from livekit-prod.yaml.example at deploy time,
# contains the real HMAC secret + redis password, NEVER commit
docker/livekit-prod.yaml

